#ifndef Server_HPP
# define Server_HPP

# include "iostream"

class Server
{
  private:
	/* data */
  protected:
	/* data */
  public:
	Server(void);
	Server(const Server &original);
	Server &operator=(const Server &original);
	~Server();
};

#endif